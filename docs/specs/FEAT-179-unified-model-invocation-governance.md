---
kind: feature-spec
feature_id: FEAT-179
title: 统一模型调用治理
status: implemented
primary_project: agentcici
task_ids: TASK-297, TASK-298, TASK-318
related_decisions: ADR-010
related_issues: ISSUE-2026-08-13-model-provider-bypass
updated_at: 2026-08-18T03:54:22Z
updated_by: codex
---

# FEAT-179 - 统一模型调用治理

## 背景与目标

当前聊天、知识库、视觉 OCR、语音识别、联网、代码解释器和部分 Skill 调用分别读取环境变量、集成应用密钥或模型厂商配置。部分代码带有具体模型默认值，缺少凭据时还会回退到默认百炼客户端。该行为会造成模型、厂商和密钥错配，且无法从平台场景模型路由审计实际调用。

本功能把所有模型调用收敛到平台受管的 `provider + model + credential` 解析结果。每一个调用场景必须有已生效的平台场景路由；厂商、模型或凭据不满足条件时明确失败，不做环境变量或其他厂商回退。

## 范围

### In Scope

- 建立唯一的服务端模型调用解析器，返回受校验的 provider、model、API 地址和凭据；调用方不得再自行拼接或读取模型密钥。
- 场景路由覆盖聊天、Skill 创作、本体建模、会议纪要、客户洞察、知识库 embedding、视觉 OCR、实时/文件 ASR、代码解释器、联网搜索和网页抓取。
- 模型厂商目录只提供已启用且显式选定的模型；embedding 与其他能力通过能力/场景路由分别校验。
- 知识库索引及检索使用 `knowledge-embedding` 场景路由；保存知识库设置与启动索引前均校验路由，避免异步 401。
- 代码解释器、联网搜索和网页抓取不再保存独立模型、API 地址或 API Key；仅保留非模型运行限制。
- 移除生产调用路径中的具体模型默认值和默认百炼客户端回退；保留测试 fixture 中的显式值。
- 为统一解析、失败关闭、知识库 embedding 和受管工具补充定向测试。
- 场景模型路由只展示并只接受具备可信、可持久化能力元数据的兼容模型；无法取得能力元数据的模型不支持专属场景路由。

### Out Of Scope

- 不自动迁移、修改或补发任何 UAT/生产厂商凭据。
- 不替用户选择供应商、模型或购买模型能力。
- 不实现未受支持模型协议的通用适配器；协议不兼容时必须拒绝配置或执行。

## 用户场景

- 平台运营为每个业务场景选择已启用、已选定且具备目标能力的模型。组织管理员只能使用这些受管结果，不能提供或覆盖模型密钥。
- 组织管理员上传知识库文档时，索引使用平台 `knowledge-embedding` 路由。路由未配置时在开始索引前返回明确错误，不调用百炼或其他默认客户端。
- Agent 调用代码解释器、联网搜索或网页抓取时，工具从对应场景路由获取模型和凭据；未配置则返回可审计的失败结果。
- 语音/视觉能力必须使用其对应场景路由；当前协议只支持的厂商与模型不匹配时拒绝执行。

## 现状与约束

- `ModelProviderService` 已是厂商凭据与模型目录的权威服务，但 `resolveRuntimeModelRoute` 仍存在默认候选和 Agent 偏好回退。
- `AliyunBailianClient` 的实现是 OpenAI-compatible HTTP 客户端，名称不能成为绕过厂商解析的理由。
- `AliyunAsrService` 当前直接读取百炼环境变量；文件 ASR 协议只支持百炼，因此路由校验必须明确其协议限制。
- 知识库当前可以保存任意 embedding provider/model；默认 local-hash 和 local profile 的百炼 embedding 都可能绕过平台场景路由。
- 现有集成应用可能包含历史的 `apiKey`、`apiBaseUrl` 和 `model` 字段；新版本不得读取它们，UI 不再显示或编辑它们。

## 方案设计

1. 在模型服务建立受治理的场景解析接口。它必须验证：路由存在、厂商已启用、模型属于该厂商的已选模型、调用协议支持该场景、地址存在且需要时密钥非空。
2. 调用方仅接收不可变的解析结果；任何解析失败抛出带稳定错误码的业务异常。通用聊天客户端只接受解析结果，不再使用环境默认地址、密钥或模型。
3. 平台模型路由增加非聊天场景。模型目录按能力过滤；OneKeyToken 可以用于其已配置且支持的聊天场景，但不会被默认为 embedding、ASR、视觉、联网或代码解释器。
4. 知识库将 embedding 模型视作路由结果。旧的知识库元数据只用于标记已有向量；重新索引时始终使用当前路由。改变 embedding 路由后须重建相应索引。
5. 受管工具配置只保留 timeout、输入限制等非敏感运行参数。工具执行前解析其场景模型，并使用平台厂商凭据。
6. 模型厂商目录允许平台运营先选择已拉取模型，不因能力尚未确认而阻止目录选择。远程目录刷新或受控检测可以持久化能力；无法自动取得时，平台运营直接人工确认能力，系统记录操作者与时间。只有能力已确认的已选模型才可进入场景候选。
7. 每个场景声明所需能力、协议限制和推荐原则。路由 API 返回已过滤候选、推荐候选和不可用原因，界面据此显示推荐说明与明确的空状态引导。

## 接口与数据影响

- 平台模型路由 API 增加上述场景；现有场景保持路径与请求格式兼容。
- 路由读取响应增加每个场景的 `requiredCapabilities`、`recommendation`、已过滤 `candidates` 与 `recommendedCandidates`；路由写入在服务端复核能力与协议兼容性。
- 厂商模型读取响应增加 `modelCapabilityConfirmations`。平台运营可通过 `PUT /platform/models/providers/{providerCode}/model-capabilities` 写入人工确认，并通过 `DELETE` 撤销；请求只包含模型与能力列表。确认与撤销写入平台审计，撤销后模型从所有场景候选中移除。
- OneKeyToken 目录通过平台已保存 Key 以 Bearer 鉴权调用配置地址下的 `GET /models`，解析 OpenAI-compatible `data[].id/name`；401 提示检查 Key 正确性或轮换，403 提示检查 Key、账号/应用状态及 `model:invoke` scope。目录结果不自动推断能力。
- 知识库 embedding 选项和保存接口改为只读显示当前路由或拒绝任意厂商/模型覆盖；历史字段保留以支持向量审计。
- 集成应用 API 对代码解释器、联网搜索、网页抓取忽略并拒绝新的独立模型凭据字段；历史密文不再参与执行。
- 不新增跨项目数据库访问或跨仓依赖。

## 验收标准

- 所有生产模型调用路径都通过统一解析器，静态扫描不再发现生产默认模型名、环境模型密钥或无凭据聊天调用。
- 每个模型调用场景没有有效路由、模型不在已选目录、厂商停用或凭据缺失时均失败关闭；不会向默认百炼地址发起请求。
- 知识库在 embedding 路由缺失时不写入向量，配置合法时使用路由返回的 provider/model/credential。
- 联网、代码解释器与网页抓取不再读取集成应用中的 API Key、地址或模型。
- 定向单元/集成测试、后端编译和前端构建通过；本轮不发布 UAT/生产。
- 全部模型目录允许先选模型，人工确认只要求至少选择一项能力；确认后只进入能力兼容场景，撤销后路由立即失败关闭并留下审计记录。

## 风险与回滚

- 强制路由会使历史未配置环境的模型能力暂时不可用，这是预期的安全失败；运营需先配置场景路由再启用能力。
- embedding 路由变更可能与既有向量维度不兼容；必须提示重建索引，不能混用向量。
- 代码解释器和联网能力具有供应商协议要求；路由指向不兼容模型时必须返回明确失败，不降级到其他模型。
- 既有已选模型可能没有可信能力元数据，升级后将不再出现在专属场景候选中，直至运营重新刷新目录或完成受控能力检测。这是预期的失败关闭行为。
- 人工确认错误可能暂时扩大候选范围，因此仅开放给平台管理员/运营角色；发现错误后应撤销确认、复核每个受影响场景并重新配置。
- 回滚仅回滚 AgentCiCi 应用版本和配置契约，不删除向量或厂商配置。

## 实现进展

- 2026-08-13：新增 `ModelInvocationResolver`。场景路由不再使用首个候选、Agent 偏好或环境变量回退；厂商初始化不再从应用环境变量注入百炼凭据。
- 2026-08-13：聊天、会议纪要、技能创作、本体建模、客户洞察、知识库/记忆 embedding、图片 OCR、实时/文件 ASR、代码解释器及联网搜索/网页抓取均已迁移到场景解析。代码解释器与联网工具配置仅保留 timeout、输入限制；迁移 V112/V113 清理知识库 local 占位和历史工具独立凭据。
- 2026-08-13：知识库管理页改为只读显示 `knowledge-embedding` 路由结果，不再提供按知识库覆盖厂商或模型的控件。后端干净编译、定向 10 项测试、前端生产构建与 diff check 均通过；UAT/生产未修改。
- 2026-08-13：用户确认模型能力元数据无法可信获得时一律视为不支持。TASK-298 已补齐受控目录/检测能力持久化、场景专属候选过滤、服务端写入及运行时校验和推荐说明。提交 `1df52ac` 已发布本地开发环境 `2.8.61-dev.1df52ac`，完整 `./stack verify` 通过；Spring 集成仍被既有测试库 Flyway V81 checksum 漂移阻断，未 repair。UAT/生产不修改。
- 2026-08-13：用户确认需要可操作的能力确认闭环。TASK-298 正在补充带厂商 HTTPS 文档与证据引用的人工确认、审计和撤销，不放宽“未知能力不可路由”的失败关闭原则；UAT/生产不修改。
- 2026-08-13：TASK-298 已提交 `f96efaf` 并发布本地开发环境 `2.8.61-dev.f96efaf`。模型目录回读证据来源，人工确认与撤销都受平台角色保护并写平台审计；撤销后服务端候选立即失效。后端 compile/package、前端定向 5 项、production build 与完整 `./stack verify` 通过；Spring 集成仍被既有 `agentcici_test` Flyway V81 checksum 漂移阻断，未 repair。受权交互和 UAT 业务验收待完成，UAT/生产未修改。
- 2026-08-13：用户要求将人工确认收敛为“选择能力并保存”，不再采集、校验或展示厂商文档与证据引用；全部模型阶段可直接加入平台目录，能力确认只影响后续场景路由。TASK-298 正在实现并回归本地开发环境，UAT/生产不修改。
- 2026-08-13：TASK-298 已提交 `1979c62` 并发布本地开发环境 `2.8.61-dev.1979c62`。人工确认 API 只接收模型和能力，审计不再记录外部证据；目录选择不再受能力确认阻塞，而场景候选仍只接受已确认的兼容能力。backend/frontend healthy/restart=0，路由 200、匿名 API 401、完整 `./stack verify` 通过；Spring 集成仍被既有 `agentcici_test` Flyway V81 checksum 漂移阻断，未 repair。UAT/生产未修改。
- 2026-08-13：截图暴露确认页把网关 HTML 直接解析为 JSON。提交 `4da7a3b` 将确认与撤销调用改为显式 JSON 协商和安全解析，非 JSON 响应返回可操作的 HTTP/版本一致性提示；本地只重建 frontend `2.8.61-dev.4da7a3b`，能力 API 保持 JSON 鉴权边界，完整 `./stack verify` 通过。UAT/生产未修改。
- 2026-08-18：TASK-318 根据 OneKeyToken 新确认的目录契约，将原 `remote-unavailable` 策略改为使用已保存 Key 调用 `GET /v1/models`；公开无效 Key 负例已确认 `401 unauthorized`。实现保留目录选择与人工能力确认分层，并补充成功、401、403 和 Key 不泄露聚焦测试；待本地 main 部署与真实正例回读。

## 交接说明

- 先阅读本规格、`ModelProviderService`、`EmbeddingService`、受管工具服务和 `AliyunAsrService`。
- 不得通过数据库直写修复任意厂商、知识库或集成应用配置。
